import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
    domains: ['images.unsplash.com', 'www.w3schools.com', 'videos.pexels.com', 'cdn.pixabay.com', 'i.imgur.com', 'images.pexels.com'], 
  },
};

export default nextConfig;
